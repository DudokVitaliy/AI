## 1. Встановлюємо CLI AWS
```
msiexec.exe /i https://awscli.amazonaws.com/AWSCLIV2.msi

aws --version
```

python --version
py -3.11 -m venv .venv
.venv\Scripts\Activate.ps1
python.exe -m pip install --upgrade pip
