python --version
py -m venv .venv

.venv\Scripts\activate.bat
python.exe -m pip install --upgrade pip

numpy — робота з масивами
matplotlib — графіки
tensorflow — нейронні мережі (включає keras)
scikit-learn — метрики (confusion_matrix, classification_report)
seaborn — красиві heatmap (матриця помилок)

pip install numpy matplotlib tensorflow scikit-learn seaborn